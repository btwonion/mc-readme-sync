{
  "name": "mc-readme-sync",
  "version": "1.1.0",
  "private": true,
  "description": "GitHub Action that syncs a Markdown file to CurseForge and Modrinth project pages",
  "main": "dist/index.js",
  "scripts": {
    "test": "node --test",
    "check": "node --check dist/index.js && node --test"
  },
  "engines": {
    "node": ">=24"
  },
  "license": "MIT"
}
